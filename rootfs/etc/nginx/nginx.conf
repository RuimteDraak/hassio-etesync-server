http {
        server {
                server_name default;

                client_max_body_size 5M;
                root /etc/server/etesync_server;

                location / {
                        uwsgi_pass unix:/etc/etesync_server.sock;
                        include uwsgi_params;
                }

                listen 80 default;
        }
}

events { 

}